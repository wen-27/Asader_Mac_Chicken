# Web API runtime.
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.31

# Validation/configuration. eval-type-backport keeps newer typing features safe
# on Python 3.9.6.
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
eval-type-backport>=0.2,<0.3

# PostgreSQL async persistence and migrations.
sqlalchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29.0
alembic>=1.13,<1.14

# Cache, locks, idempotency and semantic catalog search.
redis>=5.0,<6
chromadb>=0.5,<0.6

# LLM orchestration and graph-based conversation routing.
langchain>=0.2,<0.3
langchain-core>=0.2,<0.3
langgraph>=0.2,<0.3

# Async HTTP adapters for Telegram, Gemini and OpenRouteService.
httpx>=0.27,<0.28

# Test suite.
pytest>=8.0.0
pytest-asyncio>=0.23.0
